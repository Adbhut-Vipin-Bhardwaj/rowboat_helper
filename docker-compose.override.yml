services:
  rowboat:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  setup_qdrant:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  delete_qdrant:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  rag-worker:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  jobs-worker:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  mongo:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  redis:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  docs:
    extra_hosts:
      - "host.docker.internal:host-gateway"

  qdrant:
    extra_hosts:
      - "host.docker.internal:host-gateway"
